services:
  gateway:
    build: ./services/gateway
    ports:
      - "8000:8000"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  worker:
    build: ./services/worker
    shm_size: "3gb"